package controllers;

import models.AudioTable;
import play.mvc.Controller;

import javax.inject.Inject;

public class AudioController extends Controller {
    /*private AudioTable audios;

    @Inject
    public AudioController(AudioTable Audios) {
        this.audios = Audios;
    }
    */
}
